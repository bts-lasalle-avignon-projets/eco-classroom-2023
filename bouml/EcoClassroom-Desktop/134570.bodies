class BaseDeDonnees
!!!134570.cpp!!!	BaseDeDonnees(in type : QString)
    qDebug() << Q_FUNC_INFO << type;
    db       = QSqlDatabase::addDatabase(type);
    typeBase = type;
!!!134698.cpp!!!	~BaseDeDonnees()
    qDebug() << Q_FUNC_INFO;
!!!134826.cpp!!!	getInstance(in type : QString = TYPE_BDD) : BaseDeDonnees
    if(baseDeDonnees == nullptr)
        baseDeDonnees = new BaseDeDonnees(type);

    nbAcces++;
    qDebug() << Q_FUNC_INFO << "nbAcces" << nbAcces;

    return baseDeDonnees;
!!!134954.cpp!!!	detruireInstance() : void
    if(baseDeDonnees != nullptr)
    {
        if(nbAcces > 0)
            nbAcces--;

        qDebug() << Q_FUNC_INFO << "nbAcces restants" << nbAcces;

        if(nbAcces == 0)
        {
            delete baseDeDonnees;
            baseDeDonnees = nullptr;
        }
    }
!!!135082.cpp!!!	connecter(in nomBase : QString, in username : QString, in password : QString, in serveur : QString) : bool
    if(typeBase != "QMYSQL")
        return false;
    QMutexLocker verrou(&mutex);
    if(!db.isOpen())
    {
        db.setHostName(serveur);
        db.setUserName(username);
        db.setPassword(password);
        db.setDatabaseName(nomBase);

#ifdef DEBUG_BASEDEDONNEES
        qDebug() << Q_FUNC_INFO;
        qDebug() << "HostName : " << db.hostName();
        qDebug() << "UserName : " << db.userName();
        qDebug() << "DatabaseName : " << db.databaseName();
#endif
        if(db.open())
        {
#ifdef DEBUG_BASEDEDONNEES
            qDebug()
              << Q_FUNC_INFO
              << QString::fromUtf8("Connexion réussie à %1").arg(db.hostName());
#endif
            return true;
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : impossible de se connecter "
                                          "à la base de données !");
            QMessageBox::critical(
              nullptr,
              QString::fromUtf8("BaseDeDonnees"),
              QString::fromUtf8(
                "Impossible de se connecter à la base de données !"));
            return false;
        }
    }
    else
        return true;
!!!135210.cpp!!!	estConnecte() : bool
    QMutexLocker verrou(&mutex);
    return db.isOpen();
!!!135338.cpp!!!	ouvrir(in fichierBase : QString) : bool
    if(typeBase != "QSQLITE")
        return false;
    QMutexLocker verrou(&mutex);
    if(!db.isOpen())
    {
        db.setDatabaseName(fichierBase);

        qDebug() << Q_FUNC_INFO << db.databaseName();

        if(db.open())
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Ouverture réussie à %1")
                          .arg(db.databaseName());

            return true;
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8(
                          "Erreur : impossible d'ouvrir la base de données !");
            QMessageBox::critical(
              nullptr,
              QString::fromUtf8("BaseDeDonnees"),
              QString::fromUtf8("Impossible d'ouvrir la base de données !"));
            return false;
        }
    }
    else
        return true;
!!!135466.cpp!!!	estOuvert() : bool
    QMutexLocker verrou(&mutex);
    return db.isOpen();
!!!135594.cpp!!!	executer(in requete : QString) : bool
    QMutexLocker verrou(&mutex);
    QSqlQuery    r;
    bool         retour;

    if(db.isOpen())
    {
        if(requete.contains("UPDATE") || requete.contains("INSERT") ||
           requete.contains("DELETE"))
        {
            retour = r.exec(requete);
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Retour %1 pour la requete : %2")
                          .arg(QString::number(retour))
                          .arg(requete);
            if(retour)
            {
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !")
                          .arg(requete);
            return false;
        }
    }
    else
        return false;
!!!135722.cpp!!!	recuperer(in requete : QString, inout donnees : QString) : bool
    QMutexLocker verrou(&mutex);
    QSqlQuery    r;
    bool         retour;

    if(db.isOpen())
    {
        if(requete.contains("SELECT"))
        {
            retour = r.exec(requete);
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Retour %1 pour la requete : %2")
                          .arg(QString::number(retour))
                          .arg(requete);
            if(retour)
            {
                r.first();

                if(!r.isValid())
                {
                    qDebug() << Q_FUNC_INFO
                             << QString::fromUtf8("Résultat non valide !");
                    return false;
                }

                if(r.isNull(0))
                {
                    qDebug()
                      << Q_FUNC_INFO << QString::fromUtf8("Aucun résultat !");
                    return false;
                }
                donnees = r.value(0).toString();
                qDebug() << Q_FUNC_INFO << "Enregistrement -> " << donnees;
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !")
                          .arg(requete);
            return false;
        }
    }
    else
        return false;
!!!135850.cpp!!!	recuperer(in requete : QString, inout donnees : QStringList) : bool
    QMutexLocker verrou(&mutex);
    QSqlQuery    r;
    bool         retour;

    if(db.isOpen())
    {
        if(requete.contains("SELECT"))
        {
            retour = r.exec(requete);
            qDebug() << QString::fromUtf8(
                          "<BaseDeDonnees::recuperer(QString, QStringList)> "
                          "retour %1 pour la requete : %2")
                          .arg(QString::number(retour))
                          .arg(requete);
            if(retour)
            {
                r.first();

                if(!r.isValid())
                {
                    qDebug() << Q_FUNC_INFO
                             << QString::fromUtf8("Résultat non valide !");
                    return false;
                }

                for(int i = 0; i < r.record().count(); i++)
                    if(!r.isNull(i))
                        donnees << r.value(i).toString();
                qDebug() << Q_FUNC_INFO << "Enregistrement -> " << donnees;
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !")
                          .arg(requete);
            return false;
        }
    }
    else
        return false;
!!!135978.cpp!!!	recuperer(in requete : QString, inout donnees : QVector<QString>) : bool
    QMutexLocker verrou(&mutex);
    QSqlQuery    r;
    bool         retour;
    QString      data;

    if(db.isOpen())
    {
        if(requete.contains("SELECT"))
        {
            retour = r.exec(requete);
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Retour %1 pour la requete : %2")
                          .arg(QString::number(retour))
                          .arg(requete);
            if(retour)
            {
                while(r.next())
                {
                    data = r.value(0).toString();

                    qDebug() << Q_FUNC_INFO << "Enregistrement -> " << data;

                    donnees.push_back(data);
                }
                qDebug() << Q_FUNC_INFO << "Enregistrement -> " << donnees;
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !")
                          .arg(requete);
            return false;
        }
    }
    else
        return false;
!!!136106.cpp!!!	recuperer(in requete : QString, inout donnees : QVector<QStringList>) : bool
    QMutexLocker verrou(&mutex);
    QSqlQuery    r;
    bool         retour;
    QStringList  data;

    if(db.isOpen())
    {
        if(requete.contains("SELECT"))
        {
            retour = r.exec(requete);
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Retour %1 pour la requete : %2")
                          .arg(QString::number(retour))
                          .arg(requete);
            if(retour)
            {
                while(r.next())
                {
                    for(int i = 0; i < r.record().count(); i++)
                        data << r.value(i).toString();

                    qDebug() << Q_FUNC_INFO << "Enregistrement -> " << data;
                    for(int i = 0; i < r.record().count(); i++)
                        qDebug() << r.value(i).toString();

                    donnees.push_back(data);

                    data.clear();
                }
                qDebug() << Q_FUNC_INFO << "Enregistrement -> " << donnees;
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !")
                          .arg(requete);
            return false;
        }
    }
    else
        return false;
